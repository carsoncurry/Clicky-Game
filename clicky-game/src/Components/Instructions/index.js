import React from "react";
import "./style.css"

const Instructions = props => (
    <div className="container text-center col-md-12">
        <h3>Someone has scattered the Spiders through the Multiverse!</h3>
        <h6>Click on a spider to send them back to their home dimension!
            Make sure you click all twelve spiders once (and only once) to win the game!
        </h6>
    </div>
)

export default Instructions;
